#1.add different style and level parameters in one request (10 parameters in total), see the syn (inluding the URI in raw view) and run
#2.add more than two REST requests with more than 6 parameters each and property expansions, see the syn (inluding the URI in raw view) and run
#3.create two REST requests and only the resource and method level parameters should be inherited, not the request level
#4.change the current parameters field in request view, see the syn (inluding the URI in raw view)
#5.delete the parameters in different editors or views, see the syn (inluding the URI in raw view)